import type { NextConfig } from "next";

// Pazarlama sitesi: saf statik/SSG, PWA/SW/auth/proxy YOK (app'ten fiziksel olarak ayrı).
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
