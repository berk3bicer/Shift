# Kopyala → .env.local. Backend API kök adresi (sunucu tarafı; tarayıcıya gitmez).
API_BASE_URL=http://localhost:5203

# Pazarlama sitesi adresi (KVKK metni linki buradan türer; tarayıcıya gider).
# Deploy'da prod değeri verilir; tanımsızsa dev varsayılanı :3001 kullanılır.
NEXT_PUBLIC_MARKETING_URL=http://localhost:3001
